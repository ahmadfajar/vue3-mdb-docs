::: info <BsIcon icon="info_outlined" /><span class="ms-2 h6 mb-0">INFO</span>
Since v2.2.0, the bundle only provide contextual color variants. It is located
at `dist/theme-light.css` and `dist/theme-dark.css` files. If it doesn't suit to
your needs, you can customize it using the component's CSS variables or use the 
provided SASS mixins.
:::
